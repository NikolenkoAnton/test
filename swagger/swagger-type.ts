import { SwaggerDefinitionConstant } from 'swagger-express-ts';

export const { ARRAY, BOOLEAN, INTEGER, NUMBER, OBJECT, STRING } = SwaggerDefinitionConstant.Model.Property.Type;
export const { DATE_TIME } = SwaggerDefinitionConstant.Model.Property.Format;
